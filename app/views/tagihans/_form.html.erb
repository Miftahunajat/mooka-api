<%= form_with(model: tagihan, local: true) do |form| %>
  <% if tagihan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tagihan.errors.count, "error") %> prohibited this tagihan from being saved:</h2>

      <ul>
      <% tagihan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :jumlah %>
    <%= form.text_field :jumlah %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
